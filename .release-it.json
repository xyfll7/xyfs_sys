{
  "git": {
    "commitMessage": "config(project): 🚀release v${version}",
    "commit": true,
    "tag": true,
    "push": true
  },
  "hooks": {
    "before:git:release": "export https_proxy=http://127.0.0.1:7890 http_proxy=http://127.0.0.1:7890 all_proxy=socks5://127.0.0.1:7890 && pnpm --parallel -r --aggregate-output --filter @xyfs/* --filter @work/* --filter @ci/*  exec pnpm version ${version} && git add ."
  },
  "github": {
    "release": false
  },
  "npm": {
    "publish": false
  }
}